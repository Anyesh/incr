[package]
name = "incr-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "incr"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
incr-core = { path = "../incr-core" }
