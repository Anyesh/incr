[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "incr"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
manifest-path = "crates/incr-python/Cargo.toml"
features = ["pyo3/extension-module"]
